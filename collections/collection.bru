auth {
  mode: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

script:pre-request {
  const token = bru.getEnvVar('JWT_TOKEN');
  console.log('🔑 Token length:', token?.length || 0);
  console.log('🔑 First 10 chars:', token?.slice(0, 10));
}
